<p class="fs-5">Welcome <%= @email %>!</p>

<p class="mb-2">You can confirm your account email through the link below:</p>

<p class="mb-4"><%= link_to 'Confirm my account', confirmation_url(@resource, confirmation_token: @token), class: 'btn button-primary' %></p>
