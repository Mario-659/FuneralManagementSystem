<%= form_with(model: funeral_service, html: { class: 'needs-validation', novalidate: '' }) do |form| %>
  <% if funeral_service.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(funeral_service.errors.count, "error") %> prohibited this funeral service from being saved:</h4>
      <ul>
        <% funeral_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :description, class: 'form-label' %>
    <%= form.text_area :description, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :price, class: 'form-label' %>
    <%= form.number_field :price, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
